import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  typography: {
    fontSize: 18,
    ButtonIcon: {
      fontSize: 15
    },
    inputLabelAdd: {
      fontSize: 12
    }
  }
});

export default theme;
